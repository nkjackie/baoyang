# 解决 GitHub 推送网络连接问题

## 🔍 问题说明

错误：`fatal: unable to access 'https://github.com/nkjackie/baoyang.git/': Recv failure: Connection was reset`

**原因**：网络连接问题，可能由以下原因导致：
- 网络不稳定
- 防火墙/代理设置
- GitHub 访问受限
- 需要配置代理

---

## ✅ 解决方案

### 方案 1: 重试推送（最简单）

网络问题可能是暂时的，直接重试：

```powershell
git push -u origin main --force
```

多试几次，有时网络波动会导致连接失败。

---

### 方案 2: 检查网络连接

```powershell
# 测试 GitHub 连接
ping github.com

# 测试 HTTPS 连接
curl -I https://github.com
```

---

### 方案 3: 配置 Git 代理（如果使用代理）

如果你使用代理（VPN、公司代理等），需要配置 Git：

#### 设置 HTTP/HTTPS 代理：

```powershell
# 设置 HTTP 代理
git config --global http.proxy http://代理地址:端口

# 设置 HTTPS 代理
git config --global https.proxy https://代理地址:端口

# 示例（如果使用本地代理）
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy https://127.0.0.1:7890
```

#### 取消代理设置：

```powershell
git config --global --unset http.proxy
git config --global --unset https.proxy
```

---

### 方案 4: 增加超时时间

```powershell
# 增加 HTTP 超时时间
git config --global http.postBuffer 524288000
git config --global http.lowSpeedLimit 0
git config --global http.lowSpeedTime 999999
```

---

### 方案 5: 使用 SSH 代替 HTTPS（推荐）

如果 HTTPS 一直有问题，可以切换到 SSH：

#### 步骤：

1. **检查是否有 SSH 密钥**
   ```powershell
   ls ~/.ssh
   ```

2. **生成 SSH 密钥（如果没有）**
   ```powershell
   ssh-keygen -t ed25519 -C "你的邮箱"
   ```

3. **复制公钥**
   ```powershell
   cat ~/.ssh/id_ed25519.pub
   ```

4. **添加到 GitHub**
   - 访问：https://github.com/settings/keys
   - 点击 "New SSH key"
   - 粘贴公钥并保存

5. **修改远程仓库地址为 SSH**
   ```powershell
   git remote set-url origin git@github.com:nkjackie/baoyang.git
   ```

6. **测试 SSH 连接**
   ```powershell
   ssh -T git@github.com
   ```

7. **推送**
   ```powershell
   git push -u origin main --force
   ```

---

### 方案 6: 使用 GitHub CLI（可选）

如果命令行一直有问题，可以使用 GitHub CLI：

1. 安装 GitHub CLI：https://cli.github.com
2. 登录：`gh auth login`
3. 推送：`gh repo sync`

---

## 🎯 快速解决步骤

### 步骤 1: 先重试
```powershell
git push -u origin main --force
```

### 步骤 2: 如果还是失败，检查网络
```powershell
ping github.com
```

### 步骤 3: 如果网络正常，尝试增加超时
```powershell
git config --global http.postBuffer 524288000
git push -u origin main --force
```

### 步骤 4: 如果还是失败，切换到 SSH
```powershell
# 修改为 SSH 地址
git remote set-url origin git@github.com:nkjackie/baoyang.git

# 推送
git push -u origin main --force
```

---

## 🔧 检查当前配置

```powershell
# 查看远程仓库地址
git remote -v

# 查看 Git 配置
git config --global --list
```

---

## ❓ 常见问题

### Q: 为什么连接会被重置？

**A**: 可能原因：
- 网络不稳定
- 防火墙阻止
- 需要代理但未配置
- GitHub 在某些地区访问受限

### Q: 如何判断是否需要代理？

**A**: 
- 如果能正常访问 https://github.com，通常不需要代理
- 如果无法访问，可能需要代理或 VPN

### Q: SSH 和 HTTPS 哪个更好？

**A**: 
- **SSH**：更稳定，一次配置永久使用，适合长期使用
- **HTTPS**：简单，但可能需要 Token，网络问题可能更频繁

---

## 💡 推荐方案

1. **先重试几次**（网络波动）
2. **如果持续失败，切换到 SSH**（更稳定）
3. **如果使用代理，配置 Git 代理**
