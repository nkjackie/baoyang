# 包了么 | 顶流颜值宝贝包养平台

这是一个基于 React + Tailwind CSS 的高端网红模特中介平台前端项目。

## ✨ 特性

- **极简部署**: 纯前端架构，无需复杂的后端服务器。
- **管理后台**: 内置 `/admin` 路由，支持增删改查（数据存储在本地浏览器）。
- **图片/视频支持**: 集成 Cloudinary 支持媒体上传。
- **响应式设计**: 完美适配手机端和桌面端，霓虹深色风格。

## 🚀 快速部署

### 方式 A: Vercel (推荐)

1. 将代码推送到 GitHub。
2. 登录 [Vercel](https://vercel.com)。
3. 点击 "Add New..." -> "Project"。
4. 选择你的 GitHub 仓库。
5. Framework Preset 选择 **Vite** (如果识别不到，Build Command 填 `npm run build`，Output Directory 填 `dist`)。
6. 点击 **Deploy**。

### 方式 B: Netlify

1. 将代码推送到 GitHub。
2. 登录 [Netlify](https://www.netlify.com/)。
3. "Add new site" -> "Import an existing project"。
4. 连接 GitHub 并选择仓库。
5. Build command: `npm run build`
6. Publish directory: `dist`
7. 点击 **Deploy site**。

## 🛠 本地开发

1. 安装依赖:
   ```bash
   npm install
   ```

2. 启动项目:
   ```bash
   npm run dev
   ```

## 🔐 后台管理

访问地址: `/admin`
- **用户名**: `admin`
- **密码**: `admin123`

*注意：本演示项目的后台数据存储在浏览器的 LocalStorage 中，清除浏览器缓存会重置数据。*
