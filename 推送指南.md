# 推送到 GitHub 指南

## 📋 推送前检查清单

1. ✅ 远程仓库已配置：`https://github.com/nkjackie/baoyang.git`
2. ⬜ 文件已添加到暂存区
3. ⬜ 代码已提交
4. ⬜ 已配置 GitHub 认证（HTTPS Token 或 SSH）

---

## 🚀 推送步骤

### 步骤 1: 添加文件到暂存区

```powershell
# 添加所有文件
git add .

# 或者添加特定文件
git add 文件名
```

### 步骤 2: 提交代码

```powershell
# 提交更改（替换为你的提交信息）
git commit -m "Initial commit"

# 或者更详细的提交信息
git commit -m "feat: 初始化项目，添加基础功能"
```

### 步骤 3: 设置主分支（如果需要）

```powershell
# 如果当前分支不是 main，重命名为 main
git branch -M main
```

### 步骤 4: 推送到 GitHub

```powershell
# 第一次推送（设置上游分支）
git push -u origin main

# 之后的推送（只需要）
git push
```

---

## 🔐 认证问题

### 如果使用 HTTPS（推荐新手）

推送时会提示输入：
- **用户名**：你的 GitHub 用户名（如：nkjackie）
- **密码**：使用 **Personal Access Token**（不是 GitHub 密码）

#### 如何获取 Personal Access Token：

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. 填写 Note（如：Git Push）
4. 选择过期时间
5. 勾选权限：**至少需要 `repo` 权限**
6. 点击 "Generate token"
7. **复制 Token**（只显示一次！）

推送时，密码输入这个 Token。

### 如果使用 SSH

确保已配置 SSH 密钥：
```powershell
# 测试 SSH 连接
ssh -T git@github.com
```

如果看到 "Hi nkjackie! You've successfully authenticated..." 说明配置成功。

---

## 📝 完整推送示例

```powershell
# 1. 查看当前状态
git status

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "Initial commit: 添加项目基础文件"

# 4. 推送到 GitHub
git push -u origin main
```

---

## ❓ 常见问题

### Q: 提示 "fatal: The current branch has no upstream branch"

**解决**：使用 `-u` 参数设置上游分支
```powershell
git push -u origin main
```

### Q: 提示 "Authentication failed"

**解决**：
- HTTPS：检查 Personal Access Token 是否正确
- SSH：检查 SSH 密钥是否已添加到 GitHub

### Q: 提示 "remote: Support for password authentication was removed"

**解决**：必须使用 Personal Access Token，不能使用 GitHub 密码

### Q: 如何查看当前分支？

```powershell
git branch
```

### Q: 如何查看提交历史？

```powershell
git log --oneline
```

---

## 🎯 快速推送命令（复制粘贴）

```powershell
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main
```

---

## 📚 更多帮助

- 查看 Git 状态：`git status`
- 查看远程仓库：`git remote -v`
- 查看提交历史：`git log`
- 撤销最后一次提交：`git reset --soft HEAD~1`
